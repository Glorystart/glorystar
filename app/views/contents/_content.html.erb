<tr>
  <td><%= link_to content.name, page_content_path(@page, content) %></td>
  <td><%= link_to 'Edit', edit_page_content_path(@page, content) %></td>
  <td><%= link_to 'Destroy', page_content_path(@page, content), method: :delete, data: { confirm: 'Are you sure?' } %></td>
</tr>